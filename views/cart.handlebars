<div class="cart" style="max-width: 30rem;">

  <h2>CART</h2>

</div>
{{!-- <div class="product-card" style="max-width: 30rem;">

  <h2>product</h2>

</div> --}}

{{#if cart}}
<section>

    {{#each products as |product|}}
    <div class="card text-center mb-5">
        <div class="card-header">
            <img src="{{url_link}}" class="card-img-top" alt="...">
        </div>
        <div class="card-body">
            <h5 class="card-title">{{title}}</h5>
            <p class="card-text">{{description}}</p>
            <p>$ {{price}}</p>
            <a href="/product/{{id}}" class="btn btn-primary">Go to Product Details</a>
        </div>
    </div>
    {{/each}}

</section>

<div class="total" style="max-width: 30rem;">
  <h2>Total</h2>
</div>

{{else}}

<div class="total" style="max-width: 30rem;">
  <h4>No items in the cart.</h4>
</div>

{{/if}}
